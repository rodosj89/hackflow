version: '3.1'

services:
  mongo:
    image: mongo
    container_name: 'mongodb'
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: loop
      MONGO_INITDB_ROOT_PASSWORD: hackflow20
      MONGO_INITDB_ROOT_DATABASE: ligas
    ports:
      - '27017:27017'
